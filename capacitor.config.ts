import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rajasthan.education.nathu',
  appName: 'Rajasthan Education Nathu',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
